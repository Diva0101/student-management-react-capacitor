This is a simple react app developed to test convert web app to mobile app using capacitor

1.Clone the repo

2.Install dependencies
npm install

3.Run as a normal web app
npm run dev

4.Build web + sync capacitor
npm run build
npx cap sync android

5.Run Android(need android studio)
npx cap open android
